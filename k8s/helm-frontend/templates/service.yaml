apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-auto.fullname" . }}
  labels:
    {{- include "helm-auto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-auto.selectorLabels" . | nindent 4 }}
