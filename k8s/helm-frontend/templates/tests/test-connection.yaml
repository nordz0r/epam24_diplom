apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-auto.fullname" . }}-test-connection"
  labels:
    {{- include "helm-auto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-auto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
